#include <iostream>

using namespace std;

int fibonacci_compute(int x) {
  if (x <= 1) return 1;

  return fibonacci_compute(x-1) + fibonacci_compute(x-2);
}

int main() {
  cout<<"Finonacci of 5: " << fibonacci_compute(5)<<endl;
  return 0;
}
