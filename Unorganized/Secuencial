#include <iostream>
#include <vector>

using namespace std;

int busqSecuencial(vector<int> a, int k) { //
    int n = a.size();
    int i=0;
    while (i<n and a[i] != k) {
      i = i+1;
    }
    if (i < n) {
      return i;
    }
    else {
      return -1;
    }
}
